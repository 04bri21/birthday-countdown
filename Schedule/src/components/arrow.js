import React, { Componet }from 'react';
class Arrmow extends Componet {
    render() {
        return (
            <div> className={`${this.props.className}arrow`}>
        )
    }
}

export default Arrow